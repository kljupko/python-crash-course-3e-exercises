glossary = {
        "del": "deletes an element or key-value pair",
        "in": "checks if a value is contained in a list",
        "for": "starts a loop",
        "and": "checks if both boolean expressions are true",
        "or": "checks if at least one boolean expression is true",
        }

print(f"The 'del' keyword:\n\t{glossary['del']}")
print(f"The 'in' keyword:\n\t{glossary['in']}")
print(f"The 'for' keyword:\n\t{glossary['for']}")
print(f"The 'and' keyword:\n\t{glossary['and']}")
print(f"The 'or' keyword:\n\t{glossary['or']}")
